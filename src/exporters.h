#pragma once

#include "checker.h"

/// -------------------------------------------------------------------------------------------------
/// Exporter functions

void export_to_txt(CheckerState& state);

void export_to_csv(CheckerState& state);

/// Exporter functions
/// -------------------------------------------------------------------------------------------------
